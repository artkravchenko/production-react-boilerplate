{
  "name": "web-client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "ci": "yarn lint && yarn test",
    "lint": "eslint . --ext .js -c .eslintrc.js --color",
    "test": "jest --color"
  },
  "devDependencies": {
    "eslint": "4.19.1",
    "eslint-plugin-jest": "21.17.0",
    "jest": "23.1.0"
  }
}
